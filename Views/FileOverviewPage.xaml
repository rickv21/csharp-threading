<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FileManager.Views.FileOverviewPage"
             xmlns:local="clr-namespace:FileManager.ViewModels">
    <ContentPage.BindingContext>
        <local:FileOverviewViewModel />
        <!-- Replace YourViewModel with your actual view model class -->
    </ContentPage.BindingContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>

        <!-- Left side -->
        <Border Grid.Column="0" Margin="0, 0, 5, 0">
            <StackLayout BackgroundColor="#252526">
                <Label Text="Left Side" HorizontalOptions="Start" Margin="5, 0, 10, 5"/>
                <!-- Add label here -->

                <ScrollView VerticalScrollBarVisibility="Always">
                    <StackLayout >
                        <!-- Content for the left ScrollView -->
                        <CollectionView ItemsSource="{Binding Files}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <StackLayout Orientation="Horizontal">
                                        <StackLayout.GestureRecognizers>
                                            <DragGestureRecognizer CanDrag="True" />
                                        </StackLayout.GestureRecognizers>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <VisualState x:Name="Selected">
                                                    <VisualState.Setters>
                                                        <Setter Property="BackgroundColor" Value="LightBlue" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                        <Image Source="{Binding ImageSource}" WidthRequest="50" HeightRequest="50" />
                                        <Label Text="{Binding FileName}" />
                                    </StackLayout>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </ScrollView>
            </StackLayout>
        </Border>

        <!-- Right side -->
        <Border Grid.Column="1">
            <StackLayout BackgroundColor="#252526">
                <Entry Text="{Binding CurrentPath}" BackgroundColor="#4b4b4d" IsReadOnly="True"  Margin="5, 0, 0, 5" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="70" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label Text="" Grid.Column="0" />
                    <Label Text="Filename" Grid.Column="1" />
                    <Label Text="Type" Grid.Column="2" />
                    <Label Text="Size (MB)" Grid.Column="3" />
                </Grid>
             
                <!-- Add label here -->

                <!-- Content for the right ScrollView -->
                <CollectionView ItemsSource="{Binding Files}" SelectionMode="Multiple" x:Name="RightCollection" VerticalOptions="FillAndExpand">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.GestureRecognizers>
                                    <DragGestureRecognizer CanDrag="True" DragStarting="onDragStarting"/>
                                    <TapGestureRecognizer Tapped="OnItemTapped" />
                                    <TapGestureRecognizer NumberOfTapsRequired="2" Command="{Binding Source={x:Reference RightCollection}, Path=BindingContext.ItemDoubleTappedCommand}" CommandParameter="{Binding}" />
           
                       
                                </Grid.GestureRecognizers>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="Selected">
                                            <VisualState.Setters>
                                                <Setter Property="BackgroundColor" Value="LightBlue" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Image Source="{Binding Icon}" WidthRequest="50" HeightRequest="50" Grid.Column="0"/>
                                <Label Margin="0, 15, 20, 0" Text="{Binding FileName}" Grid.Column="1"/>
                                <Label Margin="0, 15, 0, 0" Text="{Binding FileExtension}" Grid.Column="2" />
                                <Label Margin="0, 15, 0, 0" Text="{Binding Size}" Grid.Column="3" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </Border>
    </Grid>
</ContentPage>
